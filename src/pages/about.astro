---
import MainLayout from "../layouts/MainLayout.astro";
import SectionRenderer from "../components/SectionRenderer.astro";
import UnavailablePage from "../components/UnavailablePage.astro";
import { getPageBySlug, getSiteSetting } from "../lib/api";

const [page, siteSetting] = await Promise.all([getPageBySlug("about"), getSiteSetting()]);
const title = page?.seo?.metaTitle ?? page?.title ?? siteSetting?.siteName ?? "About";
---

<MainLayout title={title} seo={page?.seo} siteSetting={siteSetting}>
  {page ? (
    <SectionRenderer sections={page.sections} />
  ) : (
    <UnavailablePage
      eyebrow="About Page"
      title="About content is not available yet"
      message="The about page shell is ready and aligned with the imported theme, but the published content has not been provided from Strapi yet."
      slug="about"
    />
  )}
</MainLayout>
